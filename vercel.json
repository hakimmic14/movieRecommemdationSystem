{
  "buildCommand": "pip install -r requirements.txt",
  "installCommand": "pip install -r requirements.txt",
  "framework": "flask",
  "functions": {
    "api/index.py": {
      "memory": 3008,
      "maxDuration": 30
    }
  }
}
