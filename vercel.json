{
  "buildCommand": "pip install -r requirements.txt",
  "installCommand": "pip install -r requirements.txt",
  "framework": "flask",
  "functions": {
    "api/index.py": {
      "memory": 2048,
      "maxDuration": 30
    }
  }
}
